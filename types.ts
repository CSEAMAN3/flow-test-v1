export type Nav = {
  href: string;
  title: string;
}

export type ServiceTab = {
  id: number;
  title: string;
  photo: string;
}

export type Review = {
  name: string;
  review: string;
  initials: string;
  background: string;
}